// E (todos operandos, sem importa a quantidade, tem que ser verdadeiros ) ( && simbolo de E)
// v e v -> v
// v e f -> f
// f e ? -> f

//Ou (basta haver um OU verdadeiro, que todos serão verdadeiros) ( || simbolo de OU )
// v ou ? -> v
// f ou v -> v 
// f ou f -> f

// xor (OU exclusivo tem q ser diferentes para serem true) ( !!(trab1 ^ trab2)) ou pode ser escrito trab1 != trab2
// v xor v -> f
// v xor f -> v
// f xor v -> v
// f xor f -> f

// !v -> f (negação de verdadeiro vira false)
// !f -> v (negação de falso vira true)